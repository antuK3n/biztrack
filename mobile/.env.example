# BizTrack mobile — API base URL.
# Simulator/web: localhost is fine. On a physical device, set your machine's LAN IP
# so the phone can reach the Laravel backend, e.g. http://192.168.1.20:8080/api/v1
EXPO_PUBLIC_API_URL=http://localhost:8080/api/v1
