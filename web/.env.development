# Dev defaults. The mock driver serves the §E1 auth contract until api/ exists;
# set VITE_USE_MOCK_API=false to hit the Laravel API instead.
VITE_API_URL=http://localhost:8080/api/v1
VITE_USE_MOCK_API=false
